#!/bin/zsh
# 0luka Catalog Shim: liam-planner
# Auto-generated by shim generator (Core Brain)

# Resolve using Core Brain Lookup Tool
REAL_PATH=$(/Users/icmini/0luka/core_brain/ops/catalog_lookup.zsh liam-planner)
EXIT_CODE=$?

if [[ $EXIT_CODE -ne 0 ]]; then
    echo "Shim Error: Could not resolve tool 'liam-planner' via catalog." >&2
    exit 1
fi

# Execute the resolved tool with original arguments
exec "$REAL_PATH" "$@"
