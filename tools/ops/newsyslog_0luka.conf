# 0luka log rotation policy
# Apply with: sudo cp tools/ops/newsyslog_0luka.conf /etc/newsyslog.d/0luka.conf
# Verify with: sudo newsyslog -v /etc/newsyslog.d/0luka.conf
#
# Format: logfilename  [owner:group]  mode  count  size(KB)  when  flags
# Flags: J=bzip2 compress, N=no-signal-needed (launchd reopens by path)
#
# logfilename                                                         mode  count  size  when  flags
/Users/icmini/0luka/observability/logs/heartbeat.log  icmini:staff   640   5      5120  *     JN
