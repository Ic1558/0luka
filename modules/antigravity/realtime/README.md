# Antigravity Realtime

Reserved for bounded realtime detection and feed-facing logic.

This directory exists to clarify that realtime detection remains module-owned,
while runtime persistence and supervision move under `runtime/`.
