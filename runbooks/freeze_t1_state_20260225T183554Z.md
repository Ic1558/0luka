# Freeze T1 State Record — 2026-02-25T18:35:54Z

## Baseline
- `NEW_BASE_TS=2026-02-25T18:35:54Z`
- `NEW_BASE_LINE=3919`

## Runtime Feed + SHA16
- `feed_path=/Users/icmini/0luka_runtime/logs/activity_feed.jsonl`
- `feed_sha16=ab46d829515ec9f7`

Command:
```bash
wc -l /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | awk '{print $1}'
```
Output:
```text
3919
```

Command:
```bash
python3 - <<'PY'
import hashlib,json
from pathlib import Path
feed=Path('/Users/icmini/0luka_runtime/logs/activity_feed.jsonl')
health=Path('/Users/icmini/0luka_runtime/logs/index/index_health.json')
h=json.loads(health.read_text())
sha=hashlib.sha256(feed.read_bytes()).hexdigest()[:16]
print('computed_sha16=',sha)
print('health_feed_sha=',h.get('feed_sha'))
print('match=',sha==h.get('feed_sha'))
print('feed_size=',feed.stat().st_size)
print('max_indexed_offset=',h.get('max_indexed_offset'))
print('status=',h.get('status'))
print('feed_path=',h.get('feed_path'))
PY
```
Output:
```text
computed_sha16= ab46d829515ec9f7
health_feed_sha= ab46d829515ec9f7
match= True
feed_size= 890894
max_indexed_offset= 890894
status= healthy
feed_path= /Users/icmini/0luka_runtime/logs/activity_feed.jsonl
```

## `index_health.json` Snippet
```json
{
  "status": "healthy",
  "feed_sha": "ab46d829515ec9f7",
  "feed_size": 890894,
  "max_indexed_offset": 890894,
  "feed_path": "/Users/icmini/0luka_runtime/logs/activity_feed.jsonl"
}
```

## Launchctl Status Proofs
Command:
```bash
launchctl list | rg 'work.0luka.dashboard.briefing' || echo briefing_unloaded_ok
```
Output:
```text
briefing_unloaded_ok
```

Command:
```bash
launchctl list | rg 'com.0luka.dispatcher' || echo dispatcher_unloaded_ok
```
Output:
```text
dispatcher_unloaded_ok
```

## Anomaly Scan (Post-Reset)
Command:
```bash
BASE_LINE=3919; tail -n +$((BASE_LINE+1)) /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | rg "system_data_integrity_risk|legacy_feed_path_redirected|rewrite_detected|index_stale|guard_violation" || echo "T1_OK_zero_anomaly_after_reset"
```
Output:
```text
T1_OK_zero_anomaly_after_reset
```

## Re-enable Protocol (R1→R2)
Rules:
- Do not use `launchctl ... | xargs ...`.
- Single-service changes only.

### Phase R1 — Sovereign Loop Only (5m scan)
Commands (do not batch):
```bash
UID="$(id -u)"
launchctl kickstart -k "gui/${UID}/com.0luka.sovereign-loop"
BASE_LINE="$(wc -l < /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | tr -d ' ')"
sleep 300
tail -n +$((BASE_LINE+1)) /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | rg "system_data_integrity_risk|legacy_feed_path_redirected|rewrite_detected|index_stale|guard_violation" || echo "R1_OK_zero_anomaly_5m"
```

### Phase R2 — Dispatcher Only (10m scan)
Commands (do not batch):
```bash
UID="$(id -u)"
launchctl kickstart -k "gui/${UID}/com.0luka.dispatcher"
BASE_LINE="$(wc -l < /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | tr -d ' ')"
sleep 600
tail -n +$((BASE_LINE+1)) /Users/icmini/0luka_runtime/logs/activity_feed.jsonl | rg "system_data_integrity_risk|legacy_feed_path_redirected|rewrite_detected|index_stale|guard_violation" || echo "R2_OK_zero_anomaly_10m"
```

### Phase R3 (Optional) — Briefing Service Only If Needed
Commands (do not batch):
```bash
UID="$(id -u)"
launchctl bootstrap "gui/${UID}" "$HOME/Library/LaunchAgents/work.0luka.dashboard.briefing.plist"
launchctl kickstart -k "gui/${UID}/work.0luka.dashboard.briefing"
```
