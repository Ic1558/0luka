# Antigravity Runtime Scheduler

This directory reserves scheduler scaffolding for future approved runtime
execution windows.

## Role

- define scheduled execution abstractions
- separate scheduling concerns from runtime mutation logic

## Non-goals

- no active scheduler registration
- no PM2 or launchd scheduling changes
