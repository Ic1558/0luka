# Legacy Redirect

This in-tree path is retired.

Use the canonical QS module instead:
- Local checkout: `/Users/icmini/0luka/repos/qs`
- GitHub: `https://github.com/Ic1558/qs`

Do not restore runtime, registry, or launch scripts to this path.
Production cutover now runs through `repos/qs` and `com.0luka.universal-qs-api`.
